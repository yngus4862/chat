{
  "uid": "compose-overview",
  "title": "Compose Overview (MVP)",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "panels": [
    {
      "type": "timeseries",
      "title": "Up",
      "gridPos": { "x": 0, "y": 0, "w": 24, "h": 8 },
      "targets": [{ "expr": "up", "legendFormat": "{{job}}:{{instance}}" }]
    }
  ]
}
